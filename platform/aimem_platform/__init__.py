"""AIMEM Design Studio control plane.

Phase 0 provides the audit backbone every later feature builds on: an
append-only, hash-chained event log, a feature registry that defines the log's
vocabulary, authentication with role-based access, and a content-addressed
artifact store.
"""

__version__ = "0.1.0"
